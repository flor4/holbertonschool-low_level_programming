#include "main.h"


/**
 * _abs - computes the absolute value of an integer
 *
 * @n: it's the int to check
 *
 * Return: 0
 *
 */


int _abs(int n)

{
	if (n > 0)
		return (n);

	else
		return (-n);

}
